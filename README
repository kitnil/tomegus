* Development environment

** Guix

   #+BEGIN_SRC sh
     GUIX_PACKAGE_PATH=$HOME/src/guix-wigust guix environment --load=guix.scm --no-grafts --root=guix-tomegus --ad-hoc gdb strace
   #+END_SRC

   #+BEGIN_SRC sh
     autoreconf -vfi
   #+END_SRC

   #+BEGIN_SRC sh
     ./configure CFLAGS="-O0 -g -Wall -Werror" LIBS="-lm"
   #+END_SRC

   #+BEGIN_SRC sh
     make
   #+END_SRC

   #+BEGIN_SRC emacs-lisp
     (local-set-key "<f5>" #'wi-compile)

     (defun wi-compile ()
       (interactive)
       (switch-to-buffer "*gud*")
       (insert "make")
       (comint-send-input))
   #+END_SRC
